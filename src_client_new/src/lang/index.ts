// Localization — mirrors src_client/lang/index.js
// Pass-through until the full i18n system is migrated.

/**
 * Returns a translated string (currently a pass-through placeholder).
 * @param text Source string or translation key.
 * @returns Translated string.
 */
export function translateText(text: string): string {
  return text
}
